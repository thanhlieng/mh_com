import React from 'react';

import AdminLayOut from '@/layout/AdminLayOut';

import PickUpContainer from './container';

const PickUp = () => {
  return <PickUpContainer />;
};
PickUp.Layout = AdminLayOut;

export default PickUp;
