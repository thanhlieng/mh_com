import OrderPickupContainer from '@/container/OrderPickupContainer';

import AdminLayOut from '@/layout/AdminLayOut';

const OrderPickupPage = () => {
  return <OrderPickupContainer />;
};
OrderPickupPage.Layout = AdminLayOut;
export default OrderPickupPage;
